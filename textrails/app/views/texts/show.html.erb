<p id="notice"><%= notice %></p>

<p>
<html>
  <head>
    <style>
      ol {
	      clear: both;
        list-style-type: none;
	padding: 0;
      }
      .square {
        display: block;
		    float: left;
        width: 50px;
        height: 50px;
      }
    </style>
  </head>
  <body>
    <ol>
      <% @text.rows.each do |row| %>
    		<li>
    		  <ol>
            <% row.each do |square| %>
              <li class="square" style='background-color:#<%= square.color %>'></li>
    		  	<% end %>
    		  </ol>
    		</li> 
      <% end %>
    </ol>
  </body>
</html>
</p>


<%= link_to 'Edit', edit_text_path(@text) %> |
<%= link_to 'Back', texts_path %>
